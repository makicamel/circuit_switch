class CreateCircuitSwitches < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :circuit_switches do |t|
      t.string :caller, null: false
      t.integer :report_count, default: 0, null: false
      # Default switch off count
      t.integer :switch_off_count, default: 10, null: false
      t.date :date_to_be_removed, default: Time.zone.now + 10.days, null: false
      t.timestamps
    end
  end
end
